DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=contextflow
DATABASE_PASSWORD=contextflow
DATABASE_NAME=contextflow
# Optional override; if set, it takes precedence over the individual DATABASE_* vars.
DATABASE_URL=postgresql+psycopg://contextflow:contextflow@localhost:5432/contextflow

# Placeholder only. Do not commit real keys.
OPENAI_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini

# Optional: set to true to create a demo source + document at startup.
SEED_DEMO_DATA=false
